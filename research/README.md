# Gate A research configuration

Versioned study configuration may live here. Participant manuscripts, contact information, consent records, raw responses, recordings, and session exports must not.

Private data belongs outside the repository in the location selected by the research lead. The root `.gitignore` rejects the common private directory names, but that is only a guardrail—not a data-management system.

