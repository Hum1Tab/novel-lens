# Credentials are intentionally not read from this file.
# OpenAI BYOK is entered for one local session and discarded after the run.
# GitHub uses the user's existing GitHub CLI / Git Credential Manager / SSH state.
